import React from "react";
import { DocumentPreviewShowcase } from "@/components/brand/DocumentPreviewShowcase";

export const metadata = {
  title: "Document Design Preview — Nafeesa Ali Brand Kit",
  description: "Screen (dark) and Print (light) pixel-matched preview for Client Proposals and Invoices.",
};

export default function DocumentPreviewPage() {
  return <DocumentPreviewShowcase />;
}
